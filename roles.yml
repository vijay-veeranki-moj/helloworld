roles:
- name: owner
  local:
    users: ["${CONCOURSE_BASIC_AUTH_USERNAME}"]
- name: member
  github:
    teams: ["${CONCOURSE_GITHUB_TEAMS}"]
- name: viewer
  github:
    orgs: ["${CONCOURSE_GITHUB_ORG}"]